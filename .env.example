# Frontend does not require local API secrets.
# Configure the Cloudflare Worker Base URL in the prenatal story assistant settings page.
# DASHSCOPE_API_KEY and any provider secrets must live only in Cloudflare Worker Secrets.

# Optional: keep empty unless the app is deployed under a sub-path.
VITE_BASE_PATH=
